# nodejs-simple-server-listing-service
Simple server listing service made with Node.js

## How to run it
`$ npm run start`

## How to set configs by the config file
* Copy `.env.example` or rename it to `.env`
* Set configs
